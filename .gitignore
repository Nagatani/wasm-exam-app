# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example
!.env.production.example
!server/.env.prod.docker.example

# Server
server/dist
server/node_modules
server/generated
# Uploaded task-statement images (server/src/lib/uploads.ts) — runtime
# content, not source; keep the directory itself out of git too, it's
# recreated on startup (fs.mkdirSync) if missing.
server/uploads

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
